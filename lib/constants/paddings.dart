import 'package:flutter/material.dart';

class ApplicationPaddings { 
  static const EdgeInsets horizontal = EdgeInsets.symmetric(horizontal: 16);
  static const EdgeInsets forButton = EdgeInsets.only(top: 32, left: 16, right: 16, bottom: 19);
  static const EdgeInsets vertical = EdgeInsets.symmetric(vertical: 9);
  static const EdgeInsets bottom = EdgeInsets.only(top:327, bottom: 10, left: 16, right: 16);
}